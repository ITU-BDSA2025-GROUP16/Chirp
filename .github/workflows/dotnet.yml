---
name: .NET Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'   # Adjust to your project's target version

      - name: Restore dependencies
        run: dotnet restore ./src/api/MyChat.Razor.csproj

      - name: Build
        run: dotnet build ./src/api/MyChat.Razor.csproj --no-restore --configuration Release

      - name: Test
        run: dotnet test ./src/api/MyChat.Razor.csproj --no-build --verbosity normal
